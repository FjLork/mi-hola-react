src/
  components/      → JSX (UI)               -> Dynpro
  styles/          → CSS                    -> Estilos
  hooks/           → JS (lógica React)      -> 
  services/        → JS (datos)             ->
  utils/           → JS (helpers)           ->